/**
 * The values the two public legal pages share.
 *
 * ⚠️ `SUPPORT_EMAIL` is a placeholder and must be a real, monitored address before submission:
 * App Store Connect requires a support URL, and a reviewer who cannot reach anyone through it is
 * a rejection under guideline 1.5. It is declared here rather than inline so it is one edit.
 *
 * `EFFECTIVE_DATE` is stamped by hand on purpose — a build-time `new Date()` would silently
 * re-date the policy on every deploy, which is the opposite of what a policy date is for.
 */
export const SUPPORT_EMAIL = "support@example.com";
export const EFFECTIVE_DATE = "15 August 2026";
